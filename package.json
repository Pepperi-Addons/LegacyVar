{
  "name": "legacy_var",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./server-side && npm run build && cd .. && cd ./client-side && npm run build && cd ..",
    "init": "npm i --force && cd ./server-side && npm i --force && cd .. && cd ./client-side && npm i --force && cd ..",
    "publish-addon": "npm run build && publish-addon --no-bump-version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pepperi-addons/scripts": "^2.0.4"
  }
}
